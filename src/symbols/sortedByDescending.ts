/**
 * Returns a copy of an array sorted using reverse order of the values selected.
 *
 * @see `sortBy`
 */
const sortedByDescending = Symbol("xnd/sortedByDescending");

export default sortedByDescending;
