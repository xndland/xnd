/**
 * Sorts an array in-place using reverse order of the values selected and returns the sorted array.
 * 
 * @see `sortBy`
 */
const sortByDescending = Symbol("xnd/sortByDescending");

export default sortByDescending;
