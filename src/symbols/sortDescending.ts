/**
 * Sorts an array in-place using reverse order of its elements and returns the sorted array.
 *
 * @see `sort`
 */
const sortDescending = Symbol("xnd/sortDescending");

export default sortDescending;
